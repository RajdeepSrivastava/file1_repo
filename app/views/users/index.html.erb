

<table>
    <tr> 
        <th> Name </th>
        <th> Email </th>
        <th> Address </th>
    </tr>

    <% @users.each do |user| %>
    <%# debugger %>

    <tr>
        <td><%= user.name %></td>
        <td><%= user.email %></td>
        <td><%= link_to "View", "/users/#{user.id}"%></td>
        <td><%= link_to "Edit", "/users/edit/#{user.id}"%> </td>
        <td><%= link_to "Delete", "/users/delete/#{user.id}" %> </td>
    <tr>
    <% end %>

</table>

<%= link_to "New User", "/users/new"%>   

